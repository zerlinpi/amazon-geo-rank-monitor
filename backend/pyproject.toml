[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "amazon-geo-rank-monitor"
version = "0.1.0"
description = "Amazon geographic organic rank monitoring core and SaaS backend"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.11,<3",
  "pydantic-settings>=2.10,<3",
  "sqlalchemy>=2.0,<3",
  "httpx>=0.28,<1",
  "PyYAML>=6.0,<7",
  "oxylabs>=3.0,<4",
  "playwright>=1.55,<2",
  "fastapi>=0.116,<1",
  "uvicorn>=0.35,<1",
  "mcp>=2,<3",
  "psycopg[binary]>=3.2,<4",
  "stripe>=15.6,<16",
]

[project.scripts]
agrm-api = "amazon_geo_rank_monitor.main:api_main"
agrm-worker = "amazon_geo_rank_monitor.main:worker_main"
agrm-bootstrap = "amazon_geo_rank_monitor.main:bootstrap_main"
agrm-mcp = "amazon_geo_rank_monitor.main:mcp_main"

[project.optional-dependencies]
dev = [
  "pytest>=8.4,<10",
  "pytest-asyncio>=1.0,<2",
  "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/amazon_geo_rank_monitor"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
